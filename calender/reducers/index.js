const initialState = {
    
}

export const TEST_REQUEST = 'TEST_REQUEST';

const reducer = (state=initialState,acion)=>{
    switch(action.type){
        case TEST_REQUEST:
        default:
            return state;
    }
}

export default reducer;